# 部署环境变量示例（复制为 .env 使用）
# 如不提供，scripts/deploy.sh 会自动生成
JWT_SECRET=change-me
# Mongo as configurable service (auto-start on deploy if port closed)
MONGO_PORT=3004
MONGO_ROOT_USER=root
MONGO_ROOT_PASS=123456
MONGO_DB_NAME=nuxt_ep_app
# If empty, deploy.sh will generate based on above, using host.docker.internal
MONGO_URI=
